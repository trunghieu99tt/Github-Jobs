import React from "react";

const SearchButton = () => {
    return (
        <button className="searchField__button" type="submit">
            Search
        </button>
    );
};

export default SearchButton;
